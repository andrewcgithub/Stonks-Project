version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
runtime-version: 3.11
pre-run:
  -pip install -r requirements.txt
command: python app.py
network:
  port: 8000

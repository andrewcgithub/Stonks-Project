version: 1.0
runtime: python3.11

build:
  commands:
    # Commands to be executed during the build phase
    - pip install -r requirements.txt

run:
  # Command to start your application
  command: python app.py
